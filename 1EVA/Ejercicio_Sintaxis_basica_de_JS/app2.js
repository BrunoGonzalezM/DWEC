let num=null; // Se inicializa la variable num con el valor null.
while (num<18){ // Mientras el valor de num sea menor que 18, se ejecuta el bucle.
    num = prompt("Ingrese una edad: "); // Se solicita al usuario que ingrese una edad y se asigna a la variable num.
}
console.log(num); // Se imprime en la consola el valor de num, que es la última edad ingresada por el usuario.